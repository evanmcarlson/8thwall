# Three.js: Afterimage postprocessing

Please note, since using a custom threejs pipeline module. Instead of XR8.Threejs.xrScene() you will reference window.xrScene, which is the same object as xrScene.
